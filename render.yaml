services:
  - type: web
    name: lottery-api
    env: node
    plan: free
    buildCommand: cd server && npm install
    startCommand: cd server && npm start
    envVars:
      - key: RENDER
        value: 'true'
    disk:
      name: lottery-data
      mountPath: /data
      sizeGB: 1
